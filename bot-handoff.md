# bot-handoff.md — ロナウジーニョBOT 引き継ぎメモ

このファイルは、ロナウジーニョBOTとClaude Code Agentが
このSEOサイトの現在地・役割分担・重要な運用前提を把握するための引き継ぎメモです。

詳細な作業ルールは `CLAUDE.md` を優先してください。

対象リポジトリ：

`ShonShonChan/seo-pages`

対象サイト：

https://revenge-casino-guide.com/

---

# 1. このBOTの目的

ロナウジーニョBOTは、
Telegramから自然文で依頼を受け取り、

- 状況を理解する
- 必要な情報を調べる
- 必要なツールを選ぶ
- GitHubリポジトリを確認する
- Claude Codeで必要な作業を行う
- 結果を検証する
- 必要に応じてPRを作成する
- Previewで確認する
- 明示承認後のみProduction反映する

ところまでを支援するAIエージェントです。

固定コマンド中心ではなく、
ユーザーが普通の日本語で目的を伝えれば、
AIが必要な手段を判断して動ける状態を目標とします。

---

# 2. 基本アーキテクチャ

基本フロー：

Telegram
↓
Cloudflare Worker
↓
強いモデルによる判断
↓
必要に応じてGitHub / Claude Code / GitHub Actions / Preview
↓
実行結果を確認
↓
Telegramへ報告

Cloudflare Workerは、
細かい作業判断を大量に持つ場所ではありません。

主な役割：

- Telegram受付
- 管理者認証
- 会話・状態の保持
- Agent起動
- 実行状態管理
- Preview / Production / Rollbackの安全制御
- 危険・不可逆操作の確認

複雑な調査、ファイル特定、修正、検証は、
可能な限りClaude Code Agentへ任せます。

---

# 3. Claude Code Agentの役割

Claude Code Agentは、
GitHubリポジトリを実際に確認・調査・修正・検証する実務担当です。

基本的な流れ：

ユーザーの目的を理解
→ Repoの現状を確認
→ 関連ファイルを調査
→ 必要な作業を実行
→ 結果を検証
→ 問題があれば修正
→ 再検証
→ 作業結果を報告

利用可能な手段の例：

- Read
- Glob
- Grep
- Bash
- curl
- Node.js
- Python
- git status
- git diff
- git log
- その他許可された検証コマンド

どのツールを使うかは、
原則としてタスク内容と現状を見てAgent側が判断します。

---

# 4. 最重要原則

このBOTでは以下を優先します。

1. ユーザーの目的を理解する
2. 会話文脈を削りすぎない
3. 推測より現物確認を優先する
4. Workerで細かく決めすぎない
5. ツール選択は可能な限りAgentへ任せる
6. 実行しただけで終わらず結果を検証する
7. 問題があれば依頼範囲内で自己修正する
8. 不可逆操作だけ安全ゲートを設ける
9. 関係ない作業へ勝手に広げない
10. 確認できない事実を断定しない

---

# 5. 会話文脈

BOTは、
現在の1メッセージだけではなく、
利用可能な文脈を使って依頼を解釈します。

対象：

- 直近の会話履歴
- 現在進行中の作業
- 最新のAgent task
- 最新のPR
- 直前の決定事項
- ユーザーが指定した制約
- 画像・スクリーンショットの情報
- 関連する永続メモリ

以下のような表現は、
可能な限り文脈から意味を解決します。

- 「それで進めて」
- 「Aで」
- 「そのPR」
- 「さっきの修正」
- 「今のやつ」
- 「前と同じ形で」

複数の解釈があり、
結果が大きく変わる場合のみ確認してください。

---

# 6. メモリ方針

メモリは2層で扱うことを基本とします。

## 短期メモリ

- 現在の会話履歴
- 進行中タスク
- 直近のPR
- 直前の作業結果

## 長期メモリ

- サイト全体の方針
- URL方針
- デザイン方針
- 重要な過去判断
- AFF運用上の決定事項
- 継続的に守るべきユーザー指定

すべての長期情報を毎回読み込むのではなく、
現在の依頼に関連する情報だけ参照する設計を優先します。

記憶と現在のRepo・実サイトが食い違う場合は、
現在確認できる現物を優先します。

---

# 7. 調査タスク

調査のみの場合は、
リポジトリファイルを変更しません。

必要に応じて：

- Glob
- Grep
- Read
- Bash
- curl
- Node.js
- Python
- `/tmp` の一時ファイル

を利用できます。

調査用の一時成果物は、
原則としてRepoへ保存しません。

ユーザーが明確な成果物を指定した場合は、
その成果物を最優先で返してください。

例：

「`.html` URLを全部出して」

なら、

最優先成果物は
**`.html` URLの一覧**

です。

関係の薄い追加分析を先に始めないでください。

---

# 8. 修正タスク

修正では、
まず対象ファイルと影響範囲を確認します。

そのうえで、
目的達成に必要なファイルだけを変更します。

同一目的のために必要であれば、
複数ファイルを1つのPRで変更して構いません。

ただし、
関係ない変更を混ぜないでください。

作業後は必ず、

- git diff
- 必要な構文確認
- リンク・画像パス
- レスポンシブ影響
- 既存仕様との整合性

など、
変更内容に合った検証を行います。

---

# 9. Git運用

通常の修正では：

- mainへ直接commitしない
- mainへ直接pushしない
- 自動マージしない
- 変更がない場合はcommitしない
- ユーザー承認なしにProduction反映しない

Telegram Claude Agent経由では、
branch作成・commit・push・PR作成は
GitHub Actions側が担当します。

Claude Code Agentは、
編集・検証・作業レポートを担当します。

読み取り目的の、

- git status
- git diff
- git log

は使用して構いません。

---

# 10. Preview / Production / Rollback

## Preview

PR作成後は、
可能な場合Preview環境で確認します。

Previewでは、
そのPRに必要な範囲だけ確認します。

問題がある場合は、
Productionへ進みません。

## Production

Production反映は、
ユーザーが明示的に承認した場合のみ実行します。

対象PRが不明確な場合は、
勝手に判断しません。

## Rollback

Rollbackは原則として
Revert PRを使用します。

mainを直接巻き戻す方法は基本的に使用しません。

---

# 11. 状態管理

BOTは最低限、以下を区別します。

- 現在実行中のAgent task
- 直前に完了したAgent task
- 直前に失敗したAgent task
- 現在確認待ちの操作
- 最新PR
- Production反映済みPR

ユーザーが、

- 「進捗は？」
- 「まだ？」
- 「今どうなってる？」
- 「さっきの作業どうなった？」

と聞いた場合は、
現在実行中のAgent taskを最優先で確認します。

過去のPRを、
現在進行中の作業として返してはいけません。

---

# 12. Legacy機能

過去の固定コマンドや旧ルートが
互換性維持のため残っている場合があります。

例：

- `github test`
- `github rules test`
- `github diagnose`
- `github scan`
- `github propose`
- `github diff`
- `github apply`

これらはLegacy機能として扱います。

現在の基本運用は、

**自然文 + 強いモデル + Claude Code Agent**

です。

Legacy機能の制限や思想を、
通常のAgent処理へ自動適用しないでください。

---

# 13. 安全境界

通常の、

- Read
- 検索
- 調査
- ローカル編集
- テスト
- git diff
- 原因特定

などの可逆的な作業は、
必要に応じてAgentが自律的に進めて構いません。

一方で、以下は安全確認が必要です。

- Production反映
- mainへの直接変更
- force push
- 大量削除
- Secret / Token操作
- 外部への不可逆送信
- その他取り返しにくい操作

---

# 14. サイト固有ルール

サイト固有の禁止表現、
デザインルール、
AFFリンク、
KYC・決済情報、
画像ルール、
Git安全ルールなどは、

**`CLAUDE.md` を最優先**

してください。

このファイルへ同じ細則を大量に重複させないでください。

---

# 15. BOTの設計思想

ロナウジーニョBOTは、

**「ユーザーが内部コマンドを覚えなくても、普通の日本語で目的を伝えれば動くAIエージェント」**

を目標とします。

設計思想：

**薄いWorker  
+ 強いモデル  
+ 現物アクセス  
+ 関連メモリ  
+ 自己検証ループ  
+ 不可逆操作だけ安全ゲート**

モデルへ届く前に、
不要な分類・要約・固定ルールで
会話文脈を削りすぎないことを重視します。
