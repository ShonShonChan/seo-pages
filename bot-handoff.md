# リベンジカジノ比較ガイド｜BOT引き継ぎ仕様書

## 1. 目的

このBOTは、Telegramからの自然文指示を受けて、
GitHubリポジトリ `ShonShonChan/seo-pages` を確認・調査・修正し、
必要に応じてPull Request作成まで行うAIエージェントです。

対象サイト：

https://revenge-casino-guide.com/

このBOTの目的は、
「決められたコマンドだけを実行するBOT」ではなく、

- ユーザーの目的を理解する
- 必要な情報を調査する
- 必要なファイルを特定する
- 必要な場合のみ修正する
- 修正内容を検証する
- 安全な形でPRを作成する
- 結果をTelegramへ返す

という一連の作業を自然文から実行できることです。

---

# 2. サイトの基本方針

サイト名：

リベンジカジノ比較ガイド

このサイトは、

- 勝てる
- 稼げる
- 負けを取り返せる
- 必ず出金できる

といった断定的・過度な訴求を目的としたサイトではありません。

「リベンジ」の意味は、

> 過去の失敗から学び、次はボーナス条件・賭け条件・出金条件・利用条件などを確認してから判断できるようになること。

として扱います。

サイト内容や表現については、
`CLAUDE.md` のルールを最優先してください。

---

# 3. 現在の基本アーキテクチャ

基本的な処理フローは以下です。

Telegram
↓
Cloudflare Worker
↓
依頼内容の理解・安全確認・状態管理
↓
必要に応じてClaude Code Agentを起動
↓
GitHubリポジトリを調査
↓
調査のみなら結果を返す
↓
修正が必要なら作業ブランチで変更
↓
検証
↓
commit / push
↓
Pull Request作成
↓
Preview確認
↓
ユーザーの明示的承認
↓
Production反映

---

# 4. Cloudflare Workerの役割

Cloudflare Workerは、
作業そのものを細かく実行する場所ではありません。

主な役割は以下です。

- Telegramからメッセージを受け取る
- ユーザー認証
- 自然文の意図を理解する
- 軽い質問・会話へ回答する
- Claude Code Agentを起動する
- 実行中タスクの状態を管理する
- 危険操作の確認を取る
- Production反映の承認を取る
- Agentの結果をTelegramへ返す

Worker側で、
ユーザーの依頼を細かすぎる固定ルールへ無理に当てはめないこと。

複雑なリポジトリ調査や、
複数ファイルにまたがる判断は、
可能な限りClaude Code Agentへ任せます。

---

# 5. Claude Code Agentの役割

Claude Code Agentは、
GitHubリポジトリに対する実作業担当です。

主な能力：

- ファイル検索
- Glob
- Grep
- Read
- HTML / CSS / Markdown等の確認
- 複数ファイルの関連確認
- URL抽出
- 内部リンク確認
- canonical確認
- sitemap確認
- SEO構造確認
- 必要に応じたHTTP確認
- curlによるステータス・リダイレクト確認
- 原因調査
- 修正案作成
- 必要なファイルのみ編集
- 検証
- 作業結果の報告

Claude Code自身は、
Telegram Claude Agent経由で実行されている場合、

- git commit
- git push
- branch操作
- PR作成

を直接行いません。

これらはGitHub Actions側が担当します。

---

# 6. 依頼の基本分類

BOTは依頼内容を大きく以下の3種類として扱います。

## A. 会話・相談

例：

- このSEO施策どう思う？
- この文章を短くして
- このタイトル案どう？
- この修正方針で問題ない？
- さっきの説明をまとめて

この場合、
GitHub全体の走査が不要であれば、
Worker内のAIで回答して構いません。

---

## B. 調査・監査

例：

- サイト内の `.html` URLを全部一覧にして
- sitemap.xmlを確認して
- canonicalの不一致を調べて
- 307になっているURLを確認して
- リダイレクトループを調べて
- 内部リンクを全部確認して
- 特定の文字列が使われているページを一覧化して
- GitHub内の関連ファイルを調べて
- 公開サイトのHTTPステータスを実測して

この場合、
**ファイル変更がなくてもClaude Code Agentを使用してよい。**

重要：

> 「変更しない依頼 = 単純な会話」

とは判断しないこと。

リポジトリ全体の検索や、
実サイト調査が必要な場合は、
調査タスクとしてClaude Code Agentを使用します。

---

## C. 修正

例：

- sitemapを直して
- canonicalを統一して
- このCTAを変更して
- HTMLを修正して
- 内部リンクを修正して
- SEO改善を反映して

この場合、
Claude Code Agentが作業ブランチ上で必要なファイルのみ変更します。

変更後は必ず検証を行います。

変更が存在する場合のみ、
GitHub Actionsがcommit / push / PR作成を行います。

---

# 7. 調査タスクのルール

調査のみを依頼された場合は、
リポジトリ内のファイルを変更しません。

必要に応じて以下を使用します。

- Glob
- Grep
- Read
- Bash
- curl
- Node.js
- Python
- `/tmp` 配下の一時ファイル

調査用の一時データを、
リポジトリ内へ保存しないこと。

---

## 成果物を最優先する

ユーザーが明確な成果物を指定している場合、
その成果物を最優先で完成させます。

例：

> `.html` URLを一覧にして

と依頼された場合、

最優先成果物は
**`.html` URLの一覧**です。

ユーザーが求めていない、

- 大規模なSEO提案
- 関係の薄い改善案
- 不要な追加分析

を先に始めないこと。

---

## 全件確認の扱い

「全部」「全件」「サイト内すべて」などが指定されている場合、
可能な限り全件確認します。

件数が多いことを理由に、
勝手に一部サンプルだけへ縮小しません。

取得できない項目がある場合は、

- 確認できた件数
- 確認できなかった件数
- 確認できなかった理由

を明示します。

---

## 調査完了時

最低限、以下を報告します。

- 実施した調査
- 対象件数
- 結果
- 問題があった対象
- 原因が特定できた場合は原因
- 不明点
- 必要であれば次の推奨作業

調査のみの場合、

- commit不要
- push不要
- PR不要

です。

---

# 8. 修正タスクのルール

修正依頼では、
まず対象ファイルと影響範囲を確認します。

そのうえで、

> 必要最小限のファイルだけを変更する

こと。

---

## 複数ファイル変更

以前の

> 1回のcommitでは原則1ページだけ変更する

という制限は、
現在のClaude Code Agentには適用しません。

同一の目的を達成するために必要であれば、
複数ファイルを1つのPRで変更して構いません。

例：

- sitemap.xml
- canonical
- 内部リンク

を同一URL方針へ統一する場合、
関連ファイルをまとめて1PRで修正して構いません。

ただし、
関係のない変更を同じPRへ混ぜないこと。

---

# 9. Git運用

## 禁止

- mainへの直接commit
- mainへの直接push
- 自動マージ
- 無断Production反映

は禁止です。

---

## 修正フロー

修正が必要な場合：

1. 作業内容確認
2. 作業ブランチ作成
3. 必要ファイル編集
4. 検証
5. git diff確認
6. GitHub Actionsがcommit
7. GitHub Actionsがpush
8. PR作成
9. Preview確認
10. Telegramへ結果・PR URL返却
11. ユーザー承認待ち
12. 承認後Production反映

---

# 10. Preview

Pull Request作成後は、
可能な場合はPreview環境で確認します。

Previewでは、

- ページ表示
- スマートフォン表示
- リンク
- HTML構造
- canonical
- sitemap
- リダイレクト
- 変更対象機能

など、
そのPRに必要な範囲を確認します。

Previewで問題が見つかった場合、
Productionへ進みません。

---

# 11. Production反映

Productionへの反映は、
必ずユーザーの明示的な承認が必要です。

例：

- 本番反映して
- このPRを本番へ
- Productionへ反映
- この内容で進めて

など、
現在の対象PRが明確で、
承認意思が確認できる場合のみ実行します。

曖昧な場合は勝手に反映しません。

---

# 12. Rollback

問題が発生した場合は、
安全なRollback手段を使用します。

原則として、

- 過去コードを直接上書き
- mainを手動で巻き戻す

のではなく、

**Revert PR**

を使用します。

Rollbackも、
対象と影響を確認してから実施します。

---

# 13. 状態管理

BOTは以下を区別して管理します。

- 現在実行中のAgent task
- 直前に完了したAgent task
- 直前に失敗したAgent task
- 現在確認待ちの操作
- 最新のPR
- Production反映済みPR

---

## 「進捗」「まだ？」への対応

ユーザーが、

- 進捗は？
- まだ？
- 今どうなってる？
- さっきの作業どうなった？

と聞いた場合、

**現在実行中のAgent taskを最優先して確認すること。**

過去の最新PRを、
現在実行中の作業として返してはいけません。

優先順位：

1. 実行中Agent task
2. 確認待ちAgent task
3. 直前のAgent task
4. 最新PR

---

# 14. 直前の作業参照

以下の自然表現を理解できるようにします。

- そのPR
- さっきのPR
- 今回の修正
- さっきの修正
- 今の調査
- 今回の調査
- さっきの作業

ただし、
参照対象が複数存在して特定できない場合は、
勝手に決めず確認します。

---

# 15. Legacyコマンド

以下の旧コマンドは、
既存互換性のため残っている場合があります。

- `github test`
- `github rules test`
- `github diagnose <page>`
- `github scan`
- `github propose <page>`
- `github diff <page>`
- `github apply <page>`

これらは
**Legacy機能**として扱います。

現在の基本運用は、
自然文 + Claude Code Agentです。

Legacyコマンドの制約を、
自然文Claude Code Agentへ自動的に適用しないこと。

例：

旧 `github apply` が1ページのみ対応だったとしても、
Claude Code Agentによる通常PR作業まで
1ページ制限する必要はありません。

---

# 16. 禁止操作

以下はユーザーの明示的指示なしに実行しません。

- mainへの直接push
- PRの自動マージ
- Production反映
- 大量ファイル削除
- 既存画像削除
- 画像ファイル名の変更
- 大規模な無関係リファクタリング
- リポジトリ全体の自動整形
- 秘密情報の表示
- APIキーやTokenの出力
- GitHub Secretsの内容表示
- Worker Secretsの内容表示

---

# 17. ユーザー指示の扱い

ユーザーの依頼目的を最優先します。

ただし、

- CLAUDE.md
- セキュリティルール
- Git安全ルール

に反する操作は実行しません。

---

## 不明確な依頼

依頼内容の一部が不明確でも、
安全に実行可能な範囲が明確であれば、
必要以上に質問せず作業を進めます。

ただし、

- Production対象が不明
- 削除対象が不明
- 大規模変更の範囲が不明
- 複数の解釈で結果が大きく変わる

場合は確認します。

---

# 18. 不要な作業を増やさない

ユーザーが依頼していない作業を、
勝手に追加しません。

例：

「URL一覧を出して」

という依頼に対して、

いきなり、

- SEO全面改修
- デザイン変更
- CTA変更
- 新規ページ追加

まで行わないこと。

必要な追加作業が見つかった場合は、

> 「追加でこの問題も確認できます」

と報告し、
勝手に実行しません。

---

# 19. 報告の品質

Telegramへの報告は、
長すぎる途中経過よりも、
ユーザーが判断できる成果を優先します。

基本構成：

1. 結論
2. 実施内容
3. 結果
4. 対象一覧
5. 問題点
6. 次に必要な作業

簡単な依頼では、
必要以上に長文にしません。

---

# 20. 現在のBOT設計方針

このBOTは、

> ユーザーがBOTの内部コマンドを覚えなくても、
> 普通の日本語で目的を伝えれば、
> AIが必要な手段を選択して作業できる

状態を目標とします。

ユーザーに毎回、

- `/agent`
- `github scan`
- `github diagnose`
- `github diff`

などの内部実装を意識させる設計にはしません。

自然文を基本インターフェースとします。

---

# 21. 設計上の原則

BOT機能を追加・修正する場合は、

**既存機能を原則維持すること。**

既存機能の削除や、
互換性を壊す変更が必要な場合は、
事前に理由を明示して確認します。

新機能は可能な限り、

- 既存機能を維持
- 新しい処理を追加
- 古い処理はLegacy化

の順で対応します。

ただし、
古い処理が現在のAgent品質を明確に低下させている場合は、
削除ではなくまず切り離し・Legacy化を検討します。

---

# 22. 最重要原則

このBOTでは、

**安全性のためにAIの能力そのものを過剰に制限しないこと。**

安全性は、

- main直接変更禁止
- Production承認
- 削除確認
- Secret保護
- 大規模変更確認

など、
危険操作の境界で担保します。

通常の、

- 調査
- 検索
- 一覧化
- 原因特定
- コード確認
- 改善案作成

については、
Claude Code Agentの能力を十分に使用できる設計とします。
