# リベンジカジノ比較ガイド｜auto-checklist.md

このファイルは、
Claude Code Agentが調査・新規ページ作成・既存ページ修正・SEO修正などを行う際の
共通チェックリストです。

対象サイト：

https://revenge-casino-guide.com/

サイト構成：

- 静的HTML / CSS / 画像
- GitHubリポジトリで管理
- Pull Request経由で変更
- Preview環境で確認
- Production反映はユーザーの明示的承認後のみ

詳細ルールは `CLAUDE.md` と `bot-handoff.md` を優先してください。

---

# 1. 作業開始前

作業開始前に以下を確認します。

- [ ] `CLAUDE.md` を確認した
- [ ] `bot-handoff.md` を確認した
- [ ] ユーザーが求めている最終成果物を理解した
- [ ] 調査のみか、修正まで必要かを確認した
- [ ] 対象ファイル・対象URL・対象ページを特定した
- [ ] 関連ファイルと影響範囲を確認した
- [ ] 既存ページの構造・デザイン・記述方法を確認した

ユーザーが明確な成果物を指定している場合、
追加分析より先にその成果物を完成させます。

例：

「.html形式のURLを一覧にして」

と指定された場合は、
まずURL一覧を完成させます。

---

# 2. 調査のみの場合

調査のみを依頼された場合は、
リポジトリ内のファイルを変更しません。

必要に応じて以下を使用できます。

- Glob
- Grep
- Read
- Bash
- curl
- Node.js
- Python
- `/tmp` 配下の一時ファイル

確認項目：

- [ ] 調査対象をすべて確認した
- [ ] 「全件」と指定された場合、可能な限り全件確認した
- [ ] 一部しか確認できなかった場合、その件数と理由を明記した
- [ ] 推測ではなく実データを確認した
- [ ] 公開サイト確認が必要な場合、実際のHTTPレスポンスを確認した
- [ ] 調査用ファイルをリポジトリ内へ残していない
- [ ] `git diff` で変更がないことを確認した

調査のみの場合：

- commit不要
- push不要
- PR不要

---

# 3. 新規HTMLページ作成時

新規ページを作成した場合は以下を確認します。

- [ ] ファイル名が英小文字を基本としている
- [ ] 単語区切りにハイフンを使用している
- [ ] 日本語ファイル名を使用していない
- [ ] スペースを含んでいない
- [ ] 既存ファイル名と重複していない
- [ ] 既存サイトの命名規則と一致している

HTMLファイル自体は `.html` を使用して構いません。

ただし、

**ファイル名と公開URLのcanonical設計は別物として扱います。**

公開URLについては、

- sitemap
- canonical
- 内部リンク
- redirect

の方針と矛盾しないことを確認します。

勝手に `.html` URLを正規URLとして決めないこと。

---

# 4. HTML基本構造

HTMLを新規作成・編集した場合は以下を確認します。

- [ ] `<!DOCTYPE html>` がある
- [ ] `<html lang="ja">` が設定されている
- [ ] charsetが設定されている
- [ ] viewportが設定されている
- [ ] `<title>` がある
- [ ] meta descriptionがある
- [ ] `<body>` がある
- [ ] ページ構造が既存サイトと整合している
- [ ] HTMLタグの閉じ忘れがない
- [ ] 不要なテンプレートコメントが残っていない
- [ ] プレースホルダーが残っていない

検索対象例：

- `{{CASINO_NAME}}`
- `{{CAMPAIGN_NAME}}`
- `{{MAIN_CONDITION}}`
- `{{WAGERING_REQUIREMENT}}`
- `{{PAYMENT_METHOD_1}}`

---

# 5. SEO基本チェック

ページ変更時は必要に応じて以下を確認します。

- [ ] titleがページ内容と一致している
- [ ] titleが不自然に長すぎない
- [ ] meta descriptionがページ内容と一致している
- [ ] meta descriptionが空ではない
- [ ] h1が適切に設定されている
- [ ] h1がページ内容と一致している
- [ ] h2 / h3の見出し構造が不自然ではない
- [ ] キーワードを過剰に詰め込んでいない
- [ ] 日本語として自然に読める
- [ ] 他ページのtitleを誤って流用していない

---

# 6. canonicalチェック

canonicalが存在するページでは以下を確認します。

- [ ] canonical URLが意図した正規URLを指している
- [ ] sitemapのURL方針と一致している
- [ ] 内部リンクのURL方針と一致している
- [ ] redirect先のURL方針と一致している
- [ ] `.html` あり / なしが混在していない

canonical方針を変更する場合は、
単一ページだけで判断せずサイト全体への影響を確認します。

---

# 7. sitemapチェック

`sitemap.xml` を変更・調査する場合は以下を確認します。

- [ ] XMLとして正しく解析できる
- [ ] `<urlset>` が正しい
- [ ] `<url>` 構造が壊れていない
- [ ] `<loc>` が正しい
- [ ] `<lastmod>` がある場合、形式が正しい
- [ ] `<changefreq>` がある場合、構造が正しい
- [ ] URL重複がない
- [ ] 404 URLが含まれていない
- [ ] 不要なredirect URLを正規URLとして掲載していない
- [ ] canonical方針と一致している

---

# 8. 内部リンクチェック

内部リンクを追加・変更する場合：

- [ ] リンク先が存在する
- [ ] 404にならない
- [ ] canonical URL方針と一致している
- [ ] 不要なredirectを経由していない
- [ ] `.html` あり / なしが不必要に混在していない
- [ ] 外部AFFリンクと内部リンクを混同していない

KONIBETなど個別のリンクルールについては、
`CLAUDE.md` を優先します。

---

# 9. 外部リンク・AFFリンク

外部AFFリンクを変更する場合：

- [ ] 正しいAFF URLを使用している
- [ ] `target="_blank"` がある
- [ ] `rel="nofollow sponsored noopener"` がある
- [ ] ユーザーから指定されていないAFFパラメータを勝手に変更していない

---

# 10. 画像チェック

画像を扱う場合：

- [ ] 既存画像を削除していない
- [ ] ファイル名を勝手に変更していない
- [ ] 参照パスを勝手に変更していない
- [ ] 画像ファイルが実在する
- [ ] alt属性が設定されている
- [ ] 装飾画像では必要に応じて `alt=""` を使用している
- [ ] モバイル表示を壊していない

---

# 11. デザインチェック

HTML / CSS変更時：

- [ ] 赤×白を基本としている
- [ ] ゴールドをアクセントに限定している
- [ ] 炎表現を過剰に使用していない
- [ ] 既存デザインと整合している
- [ ] 不要な新規ライブラリを導入していない
- [ ] モバイル表示を優先して確認した
- [ ] 横スクロールが発生していない
- [ ] タップ領域が極端に小さくない
- [ ] テキストが読みにくくなっていない

---

# 12. 表現・コンプライアンスチェック

以下のような表現が混入していないか確認します。

- 必ず勝てる
- 絶対勝てる
- 確実に勝てる
- 勝利保証
- 必ず稼げる
- 確実に稼げる
- 損失回収
- 負けを取り返す
- 取り戻せる
- 出金保証
- 必ず出金できる
- 一攫千金
- 過度な登録煽り

「リベンジ」は、
損失回収ではなく、

> 過去の失敗から学び、次は条件を確認して判断する

という意味で使用します。

---

# 13. KYC・決済情報

KYC・決済・出金条件などを記載する場合：

- [ ] 断定していない
- [ ] 情報が変更される可能性を考慮している
- [ ] 必要に応じて公式サイト確認を促している
- [ ] 古い数値を無条件で流用していない
- [ ] 出金可能性を保証する表現を使っていない

---

# 14. 大量変更・一括変更

複数ファイル変更は、
同一目的のために必要であれば実施できます。

例：

- sitemap
- canonical
- 内部リンク

を同じURL方針へ統一するための複数ファイル変更。

ただし以下を確認します。

- [ ] 変更目的が1つにまとまっている
- [ ] 関係のないファイルを変更していない
- [ ] 対象件数を変更前に確認した
- [ ] 変更後にも対象件数を確認した
- [ ] 想定外の置換が発生していない
- [ ] 全ファイルを不要に再フォーマットしていない

---

# 15. 文字コード・一括置換

日本語HTMLを編集する場合、
文字コードを壊さないことを最優先します。

特定OSや特定シェルのコマンドに依存せず、
実行環境に適した安全な方法を使用します。

確認：

- [ ] UTF-8が維持されている
- [ ] 日本語が文字化けしていない
- [ ] 改行コードだけの大量差分が発生していない
- [ ] 置換前に対象件数を確認した
- [ ] 置換後に対象文字列を再検索した
- [ ] `git diff` で余計な変更がないことを確認した

---

# 16. git diff確認

ファイル変更後は必ず `git diff` を確認します。

確認内容：

- [ ] 指示した変更だけが入っている
- [ ] 意図しないファイル変更がない
- [ ] 文字化けがない
- [ ] 不要な空白・改行変更が大量発生していない
- [ ] 既存コンテンツを誤って削除していない
- [ ] 画像パスやリンクを壊していない

---

# 17. commit / push / PR

Telegram Claude Agent経由の場合、
Claude Code自身は以下を実行しません。

- commit
- push
- branch操作
- PR作成

これらはGitHub Actions側が担当します。

Claude Codeは、

- ファイル編集
- 検証
- `git diff`確認
- 結果報告

までを担当します。

変更が存在しない場合、
commit / push / PRは作成しません。

変更が存在する場合、
GitHub Actionsが作業ブランチへcommit / pushし、
Pull Requestを作成します。

---

# 18. Preview確認

PR作成後は、
利用可能なPreview環境で変更内容を確認します。

確認内容は作業内容によって選択します。

例：

- ページが表示される
- CSSが崩れていない
- モバイル表示
- リンク
- canonical
- sitemap
- redirect
- HTTP status
- 変更対象機能

Previewで問題がある場合は、
Productionへ進めません。

---

# 19. Production反映

Production反映は、
ユーザーの明示的な承認がある場合のみ実行します。

PRを作成しただけでは、
Productionへ反映しません。

以下のような意思表示を確認します。

- 本番反映して
- このPRを本番へ
- Productionへ反映
- この内容で進めて

対象PRが不明な場合は、
勝手に判断せず確認します。

---

# 20. 最終報告

作業完了時は、
ユーザーが判断しやすい形で報告します。

調査の場合：

1. 結論
2. 対象件数
3. 調査結果
4. 対象一覧
5. 問題点
6. 次に必要な作業

修正の場合：

1. 変更内容
2. 変更ファイル
3. 検証結果
4. PR情報
5. Preview結果
6. Production反映待ちかどうか

簡単な作業では、
不要に長いレポートを作らないこと。

---

# 21. 最重要チェック

作業終了前に必ず確認します。

- [ ] ユーザーが依頼した成果物を完成させた
- [ ] 勝手に作業範囲を広げていない
- [ ] 推測だけで結論を出していない
- [ ] 必要な実データを確認した
- [ ] 不要なファイル変更をしていない
- [ ] 既存機能やコンテンツを壊していない
- [ ] `git diff` を確認した
- [ ] Productionを無断で変更していない
