# CLAUDE.md — このSEOサイトでの作業ルール

このリポジトリは静的サイト（プレーンな HTML / CSS / 画像）です。
ビルドツールやフレームワークはありません。編集した HTML がそのまま公開されます。

**対象サイト: https://revenge-casino-guide.com/**

このファイルは、このリポジトリを扱う Claude Code Agent の基本ルールです。

個別の作業指示よりも、このファイルに記載された安全・禁止ルールを優先してください。
作業指示が禁止事項に明確に反する場合は実行せず、その理由を報告してください。

一方で、このファイルを単なる「禁止事項一覧」として扱わないでください。

Claude Code は、

**ユーザーの目的を理解する  
→ 現状を確認する  
→ 必要な範囲を判断する  
→ 実行する  
→ 自分で検証する  
→ 必要なら修正する  
→ 結果を報告する**

AIエージェントとして行動してください。

---

# 0. AIエージェントとしての基本動作

Claude Code は、指示されたコードを機械的に変更するだけの作業者ではありません。

ユーザーの依頼・会話文脈・リポジトリの現状を理解したうえで、
目的達成に必要な範囲を自律的に判断し、実行・検証してください。

ただし、自律的に判断することと、依頼範囲を勝手に広げることは別です。

---

## 0-1. まずユーザーの目的を理解する

作業開始前に、

**「何を変更するよう依頼されているか」**

だけではなく、

**「ユーザーが最終的に何を達成したいのか」**

まで理解してください。

そのうえで、変更を以下の3種類に分けて考えてください。

1. ユーザーが明示的に依頼した変更
2. その変更を正しく成立させるために必要な変更
3. 単なる追加改善・リファクタリング・別問題の修正

実施してよいのは原則として **1と2のみ** です。

3については、ユーザーから明示的に依頼されていない限り勝手に実施しないでください。

### 例

ユーザーから、

> ナビゲーション上部に横長バナーを追加して

と依頼された場合、

- HTMLへのバナー追加
- 正しく表示するためのCSS追加
- スマートフォン表示への対応

は必要な変更として実施できます。

一方で、

- ナビゲーション全体の再設計
- SEOタイトル変更
- 内部リンク整理
- 他セクションのデザイン変更

まで勝手に広げてはいけません。

---

## 0-2. 推測より現物確認を優先する

確認できる情報を、推測で決めてはいけません。

特に以下については、可能な限り実際のリポジトリや実サイトを確認してください。

- ファイル名
- ディレクトリ構造
- URL
- HTML構造
- CSS
- クラス名
- 画像
- 内部リンク
- canonical
- sitemap
- 既存デザイン
- 共通パーツ
- 現在の実装仕様

判断に必要な情報が不足している場合は、まず利用可能なツールを使って調査してください。

例：

- Read
- Glob
- Grep
- git status
- git diff
- ファイル一覧
- 関連ページ確認
- 必要に応じて実サイト確認
- 利用可能な検証コマンド

**「たぶんこうなっている」ではなく、確認できるものは確認してから判断すること。**

確認できなかった情報については、推測を事実のように報告しないでください。

---

## 0-3. 会話文脈と過去の決定事項を引き継ぐ

TASK に以下の情報が渡されている場合は、
現在のユーザーメッセージだけでなく、それらも作業要件として扱ってください。

- 最近の会話履歴
- 以前決定した案
- ユーザーが指定した制約
- 前回の作業結果
- PR情報
- URL
- 画像・スクリーンショットに関する情報
- visual context
- その他の引き継ぎ情報

例えば、

- 「Aで進めて」
- 「さっきの内容で」
- 「それだけ直して」
- 「前と同じ形で」
- 「この画像みたいにして」
- 「今のやつを少しだけ変えて」

といった省略表現は、
渡されている文脈から意味を解決してください。

ただし、複数の解釈があり、結果が大きく変わる場合は、
勝手に決めて大規模な変更をしないでください。

まず確認可能な情報を調査し、
それでも一意に判断できない場合のみ、不明点として報告してください。

---

## 0-4. タスクの大きさに応じて進め方を変える

すべての作業を同じ進め方で処理しないでください。

### 小さな変更

例：

- 文言修正
- CSSの微調整
- リンク1か所の修正
- バナー1つの追加

関連ファイルと影響範囲を確認したうえで、
そのまま実装へ進んで構いません。

### 中〜大規模な変更

例：

- ページ全体の再設計
- 複数ページにまたがる変更
- ナビゲーション構造変更
- サイト全体のデザイン変更
- SEO構造変更
- 新しいページ群の作成

いきなり編集を始めず、最初に以下を整理してください。

1. 現在の構造
2. ユーザーの目的
3. 変更対象
4. 影響範囲
5. 実装方針
6. 変更しない範囲
7. 検証方法

そのうえで実装してください。

必要以上に長い計画を書く必要はありません。
目的は、編集途中で方向性を失ったり、無関係な変更へ広がったりすることを防ぐことです。

---

## 0-5. 最小変更を基本とする

ユーザーの目的を達成できる範囲で、
既存コードへの変更量はできるだけ小さくしてください。

ただし、

**「変更量を減らすこと」より「正しく完成させること」**

を優先してください。

必要なCSSやレスポンシブ対応まで削って、
中途半端な実装にしてはいけません。

一方で、

「ついでに綺麗にしておこう」

という理由だけで、

- 大規模リファクタリング
- ファイル全体の整形
- 命名変更
- 不要なコード整理
- 他ページの改善

を行わないでください。

---

## 0-6. コードを書いた時点を「完了」にしない

実装が終わっただけでは作業完了ではありません。

必ず以下のサイクルで作業してください。

**変更  
→ 検証  
→ 問題確認  
→ 必要なら修正  
→ 再検証  
→ 完了**

検証中に、今回の依頼範囲内で修正可能な問題を発見した場合は、
報告して終わるのではなく、修正して再検証してください。

ただし、依頼範囲外の問題を発見した場合は、
勝手に修正せず最終報告で知らせてください。

---

## 0-7. 実行可能な検証を自分で行う

「検証してください」と明示されていなくても、
変更内容に応じて実行可能な検証を行ってください。

変更内容に応じて、以下を確認してください。

- `git diff`
- `git status`
- HTMLタグの対応
- CSS構文
- JavaScript構文（対象の場合）
- 内部リンク先の存在
- 画像パスの存在
- 不要な変更がないか
- スマートフォン表示への影響
- デスクトップ表示への影響
- 横スクロールの原因
- CTAやナビゲーションとの競合
- canonical
- sitemap
- 禁止表現
- 既存ページとの整合性

すべてを毎回実行する必要はありません。

**今回の変更で壊れる可能性があるものを考え、適切な検証を選択してください。**

---

## 0-8. 問題を見つけたら、自分で直せるものは直す

検証中に問題を発見した場合、

「問題がありました」

と報告するだけで終了しないでください。

以下の条件をすべて満たす場合は、自分で修正してください。

- 今回の依頼範囲内である
- 原因が確認できている
- 安全に修正できる
- ユーザーの意図を変えない

修正後は必ず再検証してください。

---

## 0-9. 分からないときは、まず調査する

不明点が出た場合、

**分からない  
→ 即停止**

ではなく、

**分からない  
→ Repoを調査  
→ 関連コードを確認  
→ 必要なら実サイト確認  
→ それでも判断不能なら報告**

の順番で対応してください。

質問や確認を減らすこと自体が目的ではありません。

ただし、Claude Code自身で確認できることを、
ユーザーへ確認し直すことは避けてください。

---

## 0-10. 完了の基準

以下を満たした場合のみ、作業完了として扱ってください。

- ユーザーの依頼内容を満たしている
- ユーザーの最終目的から外れていない
- 必要以上の変更をしていない
- 既存仕様を不必要に壊していない
- `git diff` を確認済み
- 必要な検証を実行済み
- 依頼範囲内で発見した問題を修正済み
- 修正後に必要な再検証を行っている
- 確認できなかった情報を断定していない

最終報告では、簡潔に以下を説明してください。

1. 何を変更したか
2. 何を検証したか
3. 問題があった場合、どう修正したか
4. 残っている注意点や未確認事項

技術ログを大量に並べるのではなく、
ユーザーが「何が完了したのか」を理解できる報告を優先してください。

---

# 1. サイトのコンセプト —「リベンジ」の意味

このサイトの「リベンジ」は、**損失回収でも勝利保証でもありません。**

> 一度失敗した経験から学び、次は条件（ボーナス条件・出金条件・賭け条件など）を
> きちんと見てから選べるようになること。

コピーを書くときは常にこの定義に沿ってください。

「取り返す」「取り戻す」といった損失回収を連想させる表現は使わないこと。

## 禁止表現（絶対に書かない）

以下は表現を変えた言い換えも含めて禁止です。

- 勝利保証 / 必ず勝てる / 必勝 / 確実に勝てる
- 利益保証 / 儲かる / 稼げる（断定形）
- 損失回収 / 負けを取り返す / 取り戻せる
- 出金保証 / 必ず出金できる / 出金トラブルなし（断定形）
- 過度な煽り（「今すぐ登録しないと損」「これを見ないと後悔する」など）

## 推奨する書き方

- 「条件を確認してから選ぶ」
- 「自分に合う条件かを見極める」
- 断定ではなく「〜の場合があります」
- 「公式サイトで最新の条件をご確認ください」
- 18歳未満（または法定年齢未満）の利用不可
- 遊びすぎへの注意喚起

---

# 2. デザインルール

- **基本色は赤と白。金はアクセントとしてのみ使う。**
  金をベースカラーにしたり、広い面積に使ったりしない。

- **炎の演出（グラデーション・アニメーション・炎の画像）はヒーローエリアと重要なCTAに限定する。**
  本文中や一覧に多用しない。

- **モバイルファースト。**
  まずスマートフォン表示で崩れないことを意識し、そのうえでデスクトップを調整する。

- タップ領域、文字サイズ、余白、横スクロールに注意する。

- **既存デザインを尊重する。**
  既存ページと同じ構造・クラス名・トーンを確認したうえで変更する。

- 新しいデザインシステム、CSSフレームワーク、JavaScriptライブラリなどを勝手に導入しない。

- デザイン変更を依頼された場合でも、
  ユーザーが依頼していないページまで勝手に統一変更しない。

---

# 3. 画像のルール

- **既存画像のファイル名を変更しない。**
- **既存画像の参照パスを勝手に変更しない。**
- 画像の差し替え・最適化・リネームは、明示的に指示された場合のみ行う。
- `alt` 属性は必ず設定する。
- 装飾目的の画像は `alt=""` とする。
- 既存画像を削除する前に、その画像を参照している他ページがないか確認する。
- 存在を確認していない画像ファイル名を推測して使用しない。

---

# 4. リンクのルール

## 外部アフィリエイトリンク

外部の AFF リンクには必ず次を付ける。

```html
<a href="https://sloten.io?referral=0ZjM"
   target="_blank"
   rel="nofollow sponsored noopener">スロット天国をチェック</a>
