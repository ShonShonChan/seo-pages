# CLAUDE.md — このSEOサイトでの作業ルール

このリポジトリは静的サイト（プレーンな HTML / CSS / 画像）です。
ビルドツールやフレームワークはありません。編集した HTML がそのまま公開されます。

**対象サイト: https://revenge-casino-guide.com/**

このファイルのルールは、個別の作業指示より優先されます。
作業指示がここに書かれた禁止事項に反する場合は、実行せずにその旨を報告してください。

---

## 1. サイトのコンセプト —「リベンジ」の意味

このサイトの「リベンジ」は、**損失回収でも勝利保証でもありません。**

> 一度失敗した経験から学び、次は条件（ボーナス条件・出金条件・賭け条件など）を
> きちんと見てから選べるようになること。

コピーを書くときは常にこの定義に沿ってください。
「取り返す」「取り戻す」といった損失回収を連想させる表現は使わないこと。

### 禁止表現（絶対に書かない）

以下は表現を変えた言い換えも含めて禁止です。

- 勝利保証 / 必ず勝てる / 必勝 / 確実に勝てる
- 利益保証 / 儲かる / 稼げる（断定形）
- 損失回収 / 負けを取り返す / 取り戻せる
- 出金保証 / 必ず出金できる / 出金トラブルなし（断定形）
- 過度な煽り（「今すぐ登録しないと損」「これを見ないと後悔する」など）

### 推奨する書き方

- 「条件を確認してから選ぶ」「自分に合う条件かを見極める」
- 断定ではなく「〜の場合があります」「公式サイトで最新の条件をご確認ください」
- 18歳未満（または法定年齢未満）の利用不可、遊びすぎ注意といった注意喚起は残す

---

## 2. デザインルール

- **基本色は赤と白。金はアクセントとしてのみ使う。**
  金をベースカラーにしたり、広い面積に使ったりしない。
- **炎の演出（グラデーション・アニメーション・炎の画像）はヒーローエリアと
  重要な CTA に限定する。** 本文中や一覧に多用しない。
- **モバイルファースト。** まずスマートフォン表示で崩れないことを確認し、
  そのうえでデスクトップを調整する。タップ領域・文字サイズ・横スクロールに注意。
- **既存デザインを尊重する。** 既存ページと同じ構造・クラス名・トーンに合わせる。
  新しいデザインシステムやライブラリを勝手に導入しない。

---

## 3. 画像のルール

- **既存画像のファイル名を変更しない。**
- **既存画像の参照パスを勝手に変更しない。**
- 画像の差し替え・最適化・リネームは、明示的に指示された場合のみ行う。
- `alt` 属性は必ず設定する（装飾目的なら `alt=""`）。

---

## 4. リンクのルール

### 外部アフィリエイトリンク

外部の AFF リンクには必ず次を付ける。

```html
<a href="https://sloten.io?referral=0ZjM"
   target="_blank"
   rel="nofollow sponsored noopener">スロット天国をチェック</a>
```

- `target="_blank"`
- `rel="nofollow sponsored noopener"`

### サイト別

| 対象 | リンク先 | 種別 |
| --- | --- | --- |
| Slottenkoku（スロット天国） | `https://sloten.io?referral=0ZjM` | 外部AFF（上記 rel 必須） |
| KONIBET（コニベット） | `review-konibet.html` | **内部リンク**（外部AFFにしない） |

- **KONIBET は外部 AFF ではありません。** 必ず `review-konibet.html` への内部リンクにすること。
  KONIBET の公式サイトへ直接誘導する AFF リンクを作らない。
- Slottenkoku の URL は上記のもの以外を使わない（末尾のパラメータを変えない）。

### 内部リンク

- **既存の内部リンク構造を尊重する。** 既存のリンク先を勝手に張り替えない。
- 新しいリンクを追加するときは、リンク先ファイルが実在することを確認する。

---

## 5. 情報の正確性（KYC・決済）

- **KYC（本人確認）や決済の条件を断定しない。**
  手数料・反映時間・上限額・必要書類は変わります。
- 必ず「公式サイトで最新の条件をご確認ください」といった確認を促す一文を添える。
- 具体的な数値（出金上限、反映時間など）を書く場合は、
  「〜の場合があります」「記事執筆時点」など断定を避ける表現にする。

---

## 6. 作業の進め方

### 作業前

1. 作業指示に関係するファイルを特定する（`Glob` / `Grep` で検索）。
2. **影響範囲を確認する。** そのファイルを参照している他のページ、
   共通パーツ、内部リンクがないかを調べる。
3. 対象ファイルの既存の書き方（構造・クラス名・トーン）を読んでから編集する。

### 作業中

- **必要なファイルだけを編集する。** 不要なファイル変更をしない。
- 依頼されていないリファクタリング、整形、フォーマット一括変更をしない。
- このリポジトリの HTML は非常に大きいファイルが多いため、
  全体を書き直さず、必要な箇所だけを部分編集する。

### 作業後

1. **必ず `git diff` を確認する。** 意図しない変更が入っていないかを見る。
2. 検証できる範囲で検証する。
   - HTML タグの対応（開き閉じ）
   - リンク先ファイルの存在、画像パスの存在
   - 禁止表現が混入していないか
   - 既存ページとの表記ゆれ
3. 変更ファイル一覧と要点を報告する。

---

## 7. Git のルール

- **main へ直接 commit / push しない。** 例外なし。
- **必ず作業ブランチを作成し、Pull Request 経由で反映する。**
- PR は作成するだけ。自動マージしない。
- 変更がない場合は commit しない。

### 自動実行（Telegram Claude Agent）で動いている場合

`.github/workflows/telegram-claude-agent.yml` から実行されている場合、
commit / push / PR 作成はワークフロー側が行います。
**Claude 自身は git 操作を行わず、ファイル編集と検証・報告のみを担当してください。**

---

## 8. 迷ったとき

- 作業指示が不明確な場合、推測で大きな変更を加えない。
  実施できた範囲と、判断できなかった点を明記して報告する。
- 作業指示がこのファイルのルール（特に禁止表現・KONIBET の扱い・
  画像リネーム禁止）に反する場合は、実行せずに理由を報告する。
